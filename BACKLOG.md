<!-- Unlicense — cochranblock.org -->
<!-- Self-reorganizes by recency and relevance. Most important at top. -->

# Backlog

| # | Tag | Item | Depends on |
|---|-----|------|------------|
| ~~1~~ | ~~[feature]~~ | ~~Tiled matmul~~ — shipped [`0ca243d`](https://github.com/cochranblock/any-gpu/commit/0ca243d). 1.7x gain at 1024x1024 (117 GFLOPS). Further gains need register blocking + 32x32 tiles. | done |
| ~~2~~ | ~~[feature]~~ | ~~Tensor type~~ — shipped. 6-dim inline shape, reshape (zero-copy), zeros, from_buf. 8 tests. Autograd fields (requires_grad, grad_fn) deferred to Sprint 4. | done |
| ~~3~~ | ~~[feature]~~ | ~~Autograd tape~~ — shipped. Flat tape, enum Op, reverse walk. backward() for add/sub/mul/scale/relu/sigmoid/swish/tanh/matmul/mse_loss. 5 tests. | done |
| ~~4~~ | ~~[feature]~~ | ~~Backward shaders~~ — shipped relu_backward, sigmoid_backward, swish_backward, tanh_backward (4 new WGSL kernels). Remaining: softmax, group_norm, conv2d. | partial |
| ~~5~~ | ~~[feature]~~ | ~~Backward for conv2d/conv_transpose2d~~ — shipped. grad_input via conv_transpose2d (no new shader), grad_weight via new WGSL kernel, grad_bias via reduction. Tested with numeric gradient checks. | done |
| ~~6~~ | ~~[feature]~~ | ~~AdamW optimizer~~ — shipped. Single WGSL shader, bias correction, weight decay. In-place param update. 3 tests. | done |
| ~~7~~ | ~~[feature]~~ | ~~Training loop~~ — shipped. train_step() = forward + backward + optimizer. Linear regression test trains y=2x+1 from scratch. | done |
| ~~8~~ | ~~[perf]~~ | ~~Pipeline caching~~ — shipped. `Mutex<HashMap<u64, Arc<ComputePipeline>>>` in `GpuDevice`, keyed by `DefaultHasher(shader_src)`. `unary_op`, `binary_op`, `dispatch_shader`, and `AdamW::step` all route through `dev.pipeline()`. 4 new cache tests (same-Arc assertion, different-shader assertion, no-growth assertion, correctness-after-cache). 145 tests pass. | done |
| ~~S7.1~~ | ~~[feature]~~ | ~~Transformer math primitives (Sprint 7 step 1)~~ — shipped 2026-05-15. f602 layer_norm, f603 rms_norm, f563 gelu, f670 embedding_lookup, f671 argmax. 7 new WGSL shaders, 29 tests, 174 total. | done |
| ~~S7.2~~ | ~~[feature]~~ | ~~Causal SDPA + RoPE + KV cache (Sprint 7 step 2)~~ — shipped 2026-05-15. f624 apply_causal_mask, f623 scaled_dot_product_attention_causal (asymmetric q_seq_len ≤ kv_seq_len), f625 rope (adjacent-pair, start_pos + base params). t534 KVCache type with f672 new, f673 append, f674 reset, f675 cursor, f676 k_buffer, f677 v_buffer. 3 new WGSL shaders, 21 tests, 195 total. | done |
| ~~S7.3~~ | ~~[feature]~~ | ~~Safetensors loader + bf16/f16 weight ingest (Sprint 7 step 3)~~ — shipped 2026-05-16. New `src/safetensors.rs` module: t538 SafetensorsModel, f760 load-from-path (NanoSign-aware), f761 from-bytes, f762 names, f763 shape, f764 data, f765 upload-to-GPU. Free fns f766 bf16_to_f32 + f767 f16_to_f32. Deps: safetensors 0.4 + tempfile 3 (dev). 19 tests, 214 total. | done |
| ~~S7.3.1~~ | ~~[test]~~ | ~~exopack TRIPLE SIMS gate for any-gpu~~ — shipped 2026-05-16. Added optional `exopack 0.2` dep + `tests` feature, `src/bin/any-gpu-test.rs` test binary that exercises safetensors load → GPU upload → readback, RoPE determinism check (two runs must be bitwise equal), causal SDPA with first-row-equals-V[0] assertion, KV cache prefill + decode + readback. Invocation: `cargo run --release --bin any-gpu-test --features tests`. 3/3 passes on bt (RX 5700 XT, RADV/Vulkan). | done |
| ~~S7.4~~ | ~~[feature]~~ | ~~Pinned-RAM staging + layer paging from system RAM to VRAM (Sprint 7 step 4)~~ — shipped 2026-05-16. New `src/pager.rs` module: t539 LayerPager (MAP_WRITE\|COPY_SRC staging buffer in host-visible system RAM). f768 new (stage_bytes param; `DEFAULT_STAGE_BYTES` = 512 MiB covers any 13B-class tensor), f769 upload (f32 slice → VRAM; auto-chunks tensors larger than staging window), f770 page_layer (named tensors from SafetensorsModel → HashMap<String, t501>). Also fixed pre-existing flaky cache-count test (`f507_cache_grows_then_stabilizes`) to use Arc::ptr_eq instead of total-count assertion. 7 tests, 228 total. | Safetensors loader (S7.3) |
| ~~S7.5~~ | ~~[feature]~~ | ~~f16 storage type (Sprint 7 step 5)~~ — shipped 2026-05-16. t540 = GpuBufferF16 (packed 2 f16/u32). f771 upload_f16 (&[u16] → t540; odd-length zero-padded). f772 f16_to_f32 GPU kernel using WGSL `unpack2x16float` — `enable f16;` skipped (not yet in Naga/wgpu, tracked at gfx-rs/wgpu#4384). f773 pager::upload_f16_raw (chunked via staging). f774 pager::page_layer_f16 (f32→f16 on CPU via `half` crate, then staged into VRAM). s521 has_f16 added to t500 (SHADER_F16 adapter flag). dep: `half = "2"`. 5 new tests, 228→233 total. | Safetensors loader (S7.3) |
| ~~S7.6~~ | ~~[perf]~~ | ~~Flash-attention-style tiled SDPA (Sprint 7 step 6)~~ — shipped 2026-05-17. f626 `scaled_dot_product_attention_fused`: one thread per (batch_head × q_row), online softmax in a single KV pass, causal mask implicit (loop bound), output buffer as accumulator. Peak extra memory O(batch_heads × q_seq × head_dim) — no N×N scores matrix. 6 tests cross-validated against f623 (prefill, decode, partial decode, multi-head). 233→239 tests. | Causal SDPA (S7.2) |
| ~~S7.7~~ | ~~[feature]~~ | ~~Tokenizer + `Module` graph + `any-gpu serve` runtime (Sprint 7 step 7)~~ — shipped 2026-05-17. t544 Tokenizer (f775-f779, HF tokenizers wrapper). t545 Module trait + t546 Linear (f780-f781, GPU dense layer with HF weight transposition). t547 LmConfig (f782, JSON config) + t548 CausalLM (f783-f786, LLaMA/MHA+GQA forward pass using f626/f627/f628/f629). `any-gpu-serve` binary: HTTP server, POST /generate, GET /health. New ops: f627 split_heads, f628 merge_heads, f629 repeat_kv. 239→256 tests. | All above |
| ~~P1~~ | ~~[perf]~~ | ~~Wave64 matmul + 4×4 register blocking~~ — shipped 2026-05-17 [`6216f68`]. Replaced `@workgroup_size(16,16)` (4 wavefronts, costly cross-wavefront barriers) with `@workgroup_size(64)` = one wave64 on RDNA1 (barrier is a free intra-wavefront fence). Register blocking upgraded 2×2→4×4 (16 FMAs/k-step, 8 LDS reads). Result: 512² +42% (118→168 GFLOPS), 1024² +24% (118→146 GFLOPS). 256 tests pass. | done |
| ~~P2~~ | ~~[perf]~~ | ~~Subgroup-fused softmax~~ — shipped 2026-05-17 [`6216f68`]. Replaced two-dispatch two-pass (stats + apply) with single `SHADER_SOFTMAX_FUSED`: one 256-thread workgroup per row, `subgroupMax`/`subgroupAdd` for max and sum reductions across 4 subgroups via 8-slot LDS scratch. Eliminates intermediate stats buffer + inter-kernel roundtrip. `s509: bool` (has_subgroup) added to t500; auto-detected via `wgpu::Features::SUBGROUP` (true on RX 5700 XT RADV). 256 tests pass. | done |
| ~~9~~ | ~~[perf]~~ | ~~GPU-resident params across train steps~~ — shipped. `t511 GpuParams` holds `Vec<t501>` persistently. `f731` uploads once, `f732` explicit checkpoint, `f733` len. `f734 train_step_gpu`: injects resident buffers into tape via new `f681r` (Tape::leaf_resident, cheap Arc clone), collects grad bufs via `f684r` (no CPU readback), optimizer updates in-place. `wgpu::Buffer` derives `Clone` (Arc-backed). `t501`/`t540` now `#[derive(Clone)]`. 4 new tests, 298→302 total. | done |
| ~~10~~ | ~~[feature]~~ | ~~Wire `Tensor` to ops + fix README~~ — shipped. `f531` matmul (2D shape-validated), `f532` relu, `f533` softmax (2D), `f534` mse_loss (scalar [1] output), `f535` conv2d (4D NCHW, full stride/pad/dilation/groups). README test count updated (256→309). Compression map updated with f531–f535, t550=GpuParams, f731–f734. Also fixed t511 collision (GpuParams renamed t550; ElemParams retains t511). 302→309 tests. | done |
| ~~11~~ | ~~[build]~~ | ~~Stratagems CLI~~ — shipped. `src/bin/any-gpu.rs`: `any-gpu info` (adapter/backend/subgroup/f16), `any-gpu bench` (matmul prefill+decode, rms_norm, layer_norm, fused_sdpa decode+prefill at LLaMA-7B scales, compute+readback timings), `any-gpu train subatomic` (3 GPU-trained language classifiers: slop_detector, code_vs_english, lang_detector). Unknown stratagem exits with error + list. 309 tests unchanged. | done |
| ~~12~~ | ~~[feature]~~ | ~~Starter nanobyte~~ — shipped. `examples/nanobyte.rs`: NanoUNet DDPM, CHANNELS=[32,64,64], TIME_DIM=64, 2 ResBlocks/level, ~1.09M params. GPU-resident params via `f734`. Trained on 20,599 pixel-forge sprites (32×32 RGBA). CLI: `--data`, `--steps`, `--batch`, `--lr`, `--save`, `--resume`, `--sample`. PNG sample output via `image` crate. | done |
| 13 | [test] | Run 141 tests on bt/lf/gd after every push — add a `test-fleet.sh` script that SSHs all 3 nodes | — |
| 14 | [fix] | gd Intel Iris Xe untested — force `WGPU_BACKEND=vulkan` to skip NVIDIA, test Intel path in isolation | — |
| ~~15~~ | ~~[test]~~ | ~~conv2d backward stride/dilation coverage~~ — done. `f584_stride2_numeric`: stride=(2,2) numeric grad check, all 4 kernel elements. `f582_stride2_pad1`: stride=2 + pad=1 vs CPU cross-validation. | done |
| ~~16~~ | ~~[test]~~ | ~~Large tensor test (>16M elements)~~ — done. `f550_2d_dispatch`: N=16,777,216 (65536×256 workgroups), forces gid.y path in f540, checks result[0] and result[N-1]. | done |
| ~~17~~ | ~~[feature]~~ | ~~conv2d groups > 1 test~~ — done. `f582_depthwise_groups`: groups=in_c=3, identity 3×3 kernel per channel, output == input (spatial), cross-validated vs CPU. | done |
| ~~18~~ | ~~[fix]~~ | ~~`opt-level = "z"` → `opt-level = 3` in release profile~~ — done. One-liner in Cargo.toml. | done |
| ~~19~~ | ~~[fix]~~ | ~~`NanoSignResult` not enforced by compiler~~ — done. Added `#[must_use]` to `t510` enum so callers cannot silently discard the return value of `f742`/`f741`; compiler now warns on ignored result. | done |
| ~~20~~ | ~~[feature]~~ | ~~Subgroup operations for softmax~~ — done (P2). group_norm subgroup upgrade still open, see P3. | done |
| ~~21~~ | ~~[research]~~ | ~~Benchmark tiled matmul designs~~ — done (P1). 32×32 tiles + wave64 + 4×4 blocking shipped. | done |
| ~~P3~~ | ~~[perf]~~ | ~~Subgroup-fused RMSNorm + LayerNorm~~ — shipped 2026-05-17. `SHADER_RMS_NORM_FUSED`: one workgroup per row, stride-256 loop for `sum_sq`, `subgroupAdd` + 4-subgroup LDS cross-reduce, single pass (no intermediate stats buffer). `SHADER_LN_NORM_FUSED`: same but two reductions (sum→mean, sum_sq→inv_std). Gate: `s509 == true`; two-pass fallback retained. 4 new tests (stride-cols512 + many-rows for both ops). 256→260 tests. | done |
| ~~P4~~ | ~~[perf]~~ | ~~batch_matmul 4×4/wave64~~ — shipped 2026-05-17. `SHADER_BATCH_MATMUL` upgraded from `@workgroup_size(16,16)` / 1×1 blocking (256 elements/tile) to `@workgroup_size(64)` / 4×4 register blocking (1024 elements/tile, 16 FMAs/k-step). Dispatch updated m/n div_ceil(16→32). 2 new tests: tile-boundary (33×33×33×batch2) + square-64 (64×64, 2 tiles/axis). 260→262 tests. | done |
| ~~P5a~~ | ~~[perf]~~ | ~~SDPA VGPR accumulator~~ — shipped 2026-05-18 [`0c204d8`]. `SHADER_FUSED_SDPA` wrote `out[...]` on every KV step (global VRAM). Replaced with `var<private> acc_p: array<f32,128>` (per-invocation private storage = VGPRs). Eliminates 512 read-modify-write round-trips; single write at end. 512q/512kv: 369ms→138ms (2.7x). | done |
| ~~P5b~~ | ~~[perf]~~ | ~~LayerNorm 2-barrier fuse~~ — shipped 2026-05-18 [`0c204d8`]. `SHADER_LN_NORM_FUSED` used 4 workgroupBarriers (mean pass + inv_std pass). Collapsed to 2 by packing both partial sums into `wg_scratch` simultaneously (`[0..3]`=sum, `[4..7]`=sq) and computing mean+inv_std in one cross-subgroup pass. 512×4096: 13ms→3.25ms (4x), now matches RMSNorm. | done |
| 26 | [perf] | **ops_bench reference numbers** — `examples/ops_bench.rs` shipped [`0c204d8`]. Peak measured on RX 5700 XT: matmul 1116 GFLOPS (512×4096×11008), layer_norm 5.2 GB/s (512×4096), rms_norm 5.1 GB/s. See commit for full table. | done |
| ~~P6~~ | ~~[perf]~~ | ~~Wave64 fused SDPA~~ — shipped 2026-05-18 [`69c1ed0`]. `SHADER_FUSED_SDPA_W64`: one wave64 per q_row. 64 lanes own stride-64 elements of head_dim; `subgroupAdd` parallelises dot product. acc_p shrinks from 128 VGPRs to 2-4 scalars/lane. Results: decode 1q/512kv 41ms→1.1ms (38x), prefill 512q/512kv 138ms→8ms (17x), peak BW 0.5→15 GB/s. Gate: s509 && head_dim%64==0 && head_dim≤256. 3 new tests. 292→295. | done |
| ~~P7~~ | ~~[perf]~~ | ~~Two-pass GEMV for M=1 decode matmul~~ — shipped 2026-05-18. Replaced naive 1-thread-per-output (N/64=64 waves = 2.2% RDNA1 occupancy) with `SHADER_GEMV_P1`/`SHADER_GEMV_P2`: K split into K_CHUNK=256 chunks → `(ceil(N/64), ceil(K/256), 1)` P1 workgroups = ~1024 waves = 35% occupancy. P1 writes `partial[p×N + n]`; P2 reduces. Access pattern: B[k, n..n+63] is 256-byte coalesced burst per K-step; x[k] scalar broadcast stays L2-resident. Benchmark: 1×4096×4096 at ~258 GB/s effective (58% of 448 GB/s peak). 3 tests. 295→298. | done |
| ~~A3~~ | ~~[feature]~~ | ~~Sampler suite~~ — shipped 2026-05-19. `f787 top_k_mask` (k≤128, one workgroup/row, min-heap in registers), `f788 top_p_mask` (nucleus cutoff via descending sort + cumsum), `f789 sample_multinomial` (PCG RNG, deterministic given seed+step — TRIPLE SIMS safe), `f790 rep_penalty` (HF convention: pos/penalty, neg×penalty). Temperature: `f553(logits, 1/temp)`. 14 new tests, 309→323 total. | done |
| ~~B1–B6~~ | ~~[feature]~~ | ~~Transformer backward shaders~~ — shipped 2026-05-19. `f791 layer_norm_backward` (3-pass: stats, grad_input, grad_affine), `f792 rms_norm_backward` (3-pass), `f793 embedding_backward` (f32 CAS scatter-add), `f794 softmax_backward` (2-pass: dot, grad_input), `f796 rope_backward` (conjugate rotation: negated sin). New `t504` Op variants + tape wrappers `f712–f717` + backward dispatch in `f702`. 8 new tests, 323→331 total. | done |
| ~~P8~~ | ~~[perf]~~ | ~~top_k_mask speed~~ — shipped 2026-05-19 [`32e5c05`]. Rewrote SHADER_TOP_K_MASK: serial thread-0 O(V×k) → 64-thread wave64 parallel (phase 1: stride-64 scan with private 128-slot min-heap; phase 2: 8192 LDS candidates merged by thread 0; phase 3: all 64 threads apply mask). b=1 v=32k k=50: 106ms → 4.0ms (26.6×). Pipeline: 113ms → 11.8ms. | done |
| ~~B7~~ | ~~[perf]~~ | ~~embedding_backward speed~~ — shipped 2026-05-19 [`32e5c05`]. Root cause: explicit `clear_buffer(vocab×d_model)` in f793 was a redundant 512MB GPU write — wgpu zero-inits `create_buffer` per WebGPU spec. Removed. Benchmark uses vocab=512 (8MB readback) so PCIe transfer no longer swamps scatter-add time. ~5ms at vocab=512 seq=128. | done |
| 27 | [feature] | **Batched inference** — scope: (a) request scheduler with heterogeneous prompt lengths; (b) per-request KV cache slot inside t534; (c) padding attention mask extending f624 to mask padding tokens as well as causal future tokens; (d) continuous-batching eviction policy (vLLM-style). Hard dependency: A8 serve binary (done). Does not require multi-GPU. Estimated work: one new shader (padded causal mask), a slot allocator in Rust, and a batching loop in any-gpu-serve. | A8 (done) |
| 28 | [feature] | **Multi-GPU dispatch** — scope: (a) enumerate all wgpu adapters at startup, instantiate one `t500` per device; (b) tensor-parallel matmul: split K across devices, sum partial outputs (all-reduce); (c) communication primitive — wgpu has no cross-device IPC; options are mapped system RAM as rendezvous buffer (works on Resizable BAR / iGPU+dGPU) or CPU-side copy (always works, bandwidth-limited). Hard pre-requisite: routing model (#22) to decide per-op device assignment. Single-user 5700 XT already saturates 7B-class inference; defer until serving 30B+ or fleet mode. | Routing model (#22) |
| 22 | [feature] | Auto-benchmark routing model: microbench per op/size on first run, bake nanobyte .weights | Starter nanobyte (#12), kova pyramid architecture |
| 23 | [docs] | Add any-gpu to kova's module table and node deploy scripts (bt needs any-gpu for pixel-forge training) | kova docs |
| 24 | [feature] | Multi-GPU dispatch: split work across discrete + integrated by measured throughput (see #28 for full scope) | Routing model (#22) |
| 25 | [build] | Publish to crates.io: `cargo publish` — needs license file check, README examples, docs.rs badges | Tensor ops (#10) |

---

Part of [The Cochran Block](https://cochranblock.org) — see also [kova](https://github.com/cochranblock/kova), [pixel-forge](https://github.com/cochranblock/pixel-forge)
<!-- COCHRANBLOCK-BRAND-FOOTER:START - generated by cochranblock/scripts/brand-stamp.sh -->

---

<sub>&#9656; **THE COCHRAN BLOCK, LLC** &#183; CAGE `1CQ66` &#183; UEI `W7X3HAQL9CF9` &#183; UNLICENSE &#183; [cochranblock.org](https://cochranblock.org)</sub>
<!-- COCHRANBLOCK-BRAND-FOOTER:END -->
